node_modules/
.turbo/
.cache/
*.local
.env
.DS_Store
coverage/
storybook-static/
*.tsbuildinfo

# dist is gitignored for most packages — only packages/tokens/dist is committed
# (CI checks that it matches the source with git diff --exit-code)
dist/
!packages/tokens/dist/

# Local debug artifacts (e.g. `pnpm figma:payload` writes here)
.debug/
