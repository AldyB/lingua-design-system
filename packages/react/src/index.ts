/**
 * @lingua/react — Phase 1 entry point
 *
 * Components are ported and cleaned up in Phase 3.
 * Handoff reference files live in src/handoff/ and are excluded from the build.
 */

// Phase 3 will export components here, e.g.:
// export { FlashcardFlipper } from './components/FlashcardFlipper';
// export { BottomNav } from './components/BottomNav';

export const version = '0.0.1';
